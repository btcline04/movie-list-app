<%= simple_form_for @list do |f| %>
  <div class="form-inline">
    <div class="form-group">
      <%= f.input :name, label: false, placeholder: "List Name", input_html: { class: 'form-control' } %>
    </div>

    <div class="form-group">
        <%= button_tag(type: 'submit', class: "btn btn-primary btn-space") do %> Create Movie List <% end %> 
    </div>
  </div>
<% end %>
